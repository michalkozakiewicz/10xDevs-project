---
// Redirect to sessions page
// - If user is not authenticated: middleware will redirect to /auth/login
// - If user is authenticated: will show sessions dashboard
return Astro.redirect("/sessions");
---
